services:
  goloop:
    image: ghcr.io/cookieboty/goloop:latest
    ports:
      - "8080:8080"
    volumes:
      - goloop-images:/tmp/images
    environment:
      TZ: Asia/Shanghai
      KIEAI_BASE_URL: ${KIEAI_BASE_URL}
      STORAGE_BASE_URL: ${STORAGE_BASE_URL}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  goloop-images:
    driver: local
